// javaScritp types are: string, number, boolean, undefined, null, bigint, symbol
// these types are valid in typeScript

let testString: string = 'test';
let testNumber: number = 123;
let testBool: boolean = false;

// defined variable types can't assing to another types. uncomment below code to see.

// testBool = 133
// testString = false
// testNumber = null